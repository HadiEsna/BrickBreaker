#include "import_struct.h"void check() {    for (int i = 0; i < 2 * n; i++) {        if (block[3 * n - 1][i] > 0) {            play = 0;            return;        }    }}void pull_down() {    int i, j;    for (i = 3 * n; i > 0; i--) {        for (j = 0; j < 2 * n; j++) {            block[i][j] = block[i - 1][j];        }    }    check();    for (i = 0; i < 2 * n; i++) {        block[0][i] = 0;    }    if (play == 1) {        int t_b = rand() % (2 * n - 2) + 1;        for (i = 0; i < t_b;) {            int x = rand() % (2 * n);            if (!block[0][x]) {                block[0][x] = rand() % (score) + 1;                i++;            }        }        int g = rand() % (2 * n);        if (g + t_b > 2 * n) {            while (1) {                int x = rand() % (2 * n);                if (!block[0][x]) {                    block[0][x] = -1;                    break;                }            }        }        for (i = 0; i < 2 * n; i++) {            if (block[1][i] < 0)                check_bomb(1, i);        }    }}